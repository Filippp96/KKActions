name: Third Workflow

on: 
    workflow_dispatch:
    push:


jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repository code
              uses: actions/checkout@v4

            - name: Script execution
              run: |
                ls -la
                chmod +x ascii.sh
                ls -la
                ./ascii.sh

    second_job:
        runs-on: ubuntu-latest
        steps:
            - name: Script execution
              run: |
                ls -la
                sleep 5